import React, {useState} from "react";

export const ProductList = () => {
    // 1) Form state (for add / edit) --

    // 2) List of products (in-memory) ---

    // 


    return(
        <div>
            <h1>ProductList Crud Operations </h1>
        </div>
    )
}